#include "main.h"

/**
 * _strlen - string length
 *
 * @s: string operand
 *
 * Return: l
 *
 */

int _strlen(char *s)
{
	int l = 0;
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		l++;
	}
	return (l);
}
