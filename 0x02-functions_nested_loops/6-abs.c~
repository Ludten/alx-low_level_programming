#include "main.h"

/**
 * print_sign - check for polarity.
 *
 * @n: input variable
 * Return: p
 */

int print_sign(int n)
{
	int p;

	if (n > 0)
	{
		_putchar('+');
		p = 1;
	}
	else if (n < 0)
	{
		_putchar('-');
		p = -1;
	}
	else
	{
		_putchar('0');
		p = 0;
	}

	return (p);
}
